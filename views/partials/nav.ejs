<nav class="bg-gradient-to-r from-indigo-900 via-red-800 to-amber-400">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      <div class="flex items-center">
        <a href="/" class="flex items-center space-x-2 text-white font-bold text-xl">
          <i class="fas fa-blog"></i>
          <span>Blogify</span>
        </a>

        <div class="hidden md:block ml-10">
          <div class="flex items-center space-x-4">
            <a href="/" class="text-white hover:bg-white/10 px-3 py-2 rounded-md">
              <i class="fas fa-home mr-2"></i>Home
            </a>

            <% if (locals.user) { %>
              <a href="/blog/add-new-blog" class="text-white hover:bg-white/10 px-3 py-2 rounded-md">
                <i class="fas fa-plus-circle mr-2"></i>New Blog
              </a>
              <% } %>
                <% if (locals.user) { %>
                  <% } %>
          </div>
        </div>
      </div>

      <div class="flex items-center space-x-4">
        <div class="relative">
          <input type="search"
            class="w-64 px-4 py-2 rounded-lg bg-white/10 text-white placeholder-white/75 focus:outline-none focus:ring-2 focus:ring-white/50"
            placeholder="Search posts..." />
          <button class="absolute right-2 top-2.5 text-white">
            <i class="fas fa-search"></i>
          </button>
        </div>

        <% if (locals.user) { %>
          <div class="dropdown">
            <button class="btn dropdown-toggle border-0 flex items-center gap-2 hover:bg-white/10 bg-white/10"
              type="button" data-bs-toggle="dropdown" aria-expanded="false">
              <img src="<%= locals.user.profileImage %>" alt="<%= locals.user.fullname %>" class="w-8 h-8 rounded-full" />
              <%= locals.user.fullname %>
            </button>
            <ul class="dropdown-menu">
              <li><a class="dropdown-item" href="/user/profile">Profile</a></li>
              <li><a class="dropdown-item" href="/user/signout">Logout</a></li>
            </ul>
          </div>
          <% } else { %>
            <a href="/user/signin" class="text-white hover:bg-white/10 px-3 py-2 rounded-md">
              <i class="fas fa-user mr-2"></i>Sign In
            </a>
            <% } %>
      </div>
    </div>
  </div>
</nav>

<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
  <% if (locals.error) { %>
    <div class="alert alert-danger" role="alert">
      <%= locals.error %>
    </div>
    <% } %>
</div>