<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./partials/head') %>
    <title>Join Blogify - Create Account</title>
  </head>
  <body class="bg-gray-50">
    <%- include('./partials/nav') %>

    <div
      class="min-h-[80vh] flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8"
    >
      <div
        class="max-w-md w-full space-y-8 bg-white p-10 rounded-2xl shadow-xl"
      >
        <div class="text-center">
          <h2 class="text-4xl font-extrabold text-blue-900 tracking-tight">
            Create Account
          </h2>
          <p class="mt-2 text-gray-600">
            Join our community of writers and readers
          </p>
        </div>

        <form class="mt-8 space-y-6" action="/user/signup" method="POST">
          <div class="space-y-6">
            <div>
              <label class="block text-sm font-medium text-gray-700"
                >Full Name</label
              >
              <input
                type="text"
                name="fullname"
                required
                class="mt-1 block w-full px-4 py-3 border border-gray-300 rounded-xl shadow-sm focus:ring-blue-500 focus:border-blue-500"
              />
            </div>

            <div>
              <label class="block text-sm font-medium text-gray-700"
                >Email address</label
              >
              <input
                type="email"
                name="email"
                required
                class="mt-1 block w-full px-4 py-3 border border-gray-300 rounded-xl shadow-sm focus:ring-blue-500 focus:border-blue-500"
              />
            </div>

            <div>
              <label class="block text-sm font-medium text-gray-700"
                >Password</label
              >
              <input
                type="password"
                name="password"
                required
                class="mt-1 block w-full px-4 py-3 border border-gray-300 rounded-xl shadow-sm focus:ring-blue-500 focus:border-blue-500"
              />
            </div>
          </div>

          <button
            type="submit"
            class="w-full flex justify-center py-3 px-4 border border-transparent rounded-xl shadow-sm text-sm font-medium text-white bg-blue-900 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transform transition hover:-translate-y-0.5"
          >
            Create Account
          </button>

          <p class="mt-6 text-center text-sm text-gray-600">
            Already have an account?
            <a
              href="/user/signin"
              class="font-medium text-blue-900 hover:text-blue-500"
            >
              Login here
            </a>
          </p>
        </form>
      </div>
    </div>

    <%- include('./partials/scripts') %>
  </body>
</html>
